services:
  avito-moderation:
    image: avito-moderation
    ports:
      - "6969:80"
    environment:
      - BACKEND_HOST=host.docker.internal
      - BACKEND_PORT=3001
    extra_hosts:
      - "host.docker.internal:host-gateway"
